program mouse_test;
{$i ../core/mouse.simba}

(*
mouse_test
~~~~~~~~~~

Description:
  This file is used to test the mouse functions

*)

(*
GetMousePoints
~~~~~~~~~~~~~~

Description:
  Random points used for testing the accuracy and movement of
  the mouse functions.

Example:
  var
    points : Array[0..19] of TPoint;
    ...
    points := GetMousePoints();

Returns:
  Returns an Array[0..19] of TPoint

*)

function GetMousePoints() : Array[0..19] of TPoint;
var
  points : Array[0..19] of TPoint;

begin

  points[0] := [206, 347];
  points[1] := [29, 59];
  points[2] := [1745, 373];
  points[3] := [790, 298];
  points[4] := [1634, 120];
  points[5] := [1164, 389];
  points[6] := [954, 721];
  points[7] := [131, 379];
  points[8] := [941, 104];
  points[9] := [71, 254];
  points[10] := [1132, 318];
  points[11] := [428, 336];
  points[12] := [298, 187];
  points[13] := [640, 272];
  points[14] := [1419, 632];
  points[15] := [533, 100];
  points[16] := [1376, 516];
  points[17] := [1287, 737];
  points[18] := [319, 578];
  points[19] := [1819, 706];

  result := points;

end;

(*
TestMouse
~~~~~~~~~

Description:
  This method is used to test the movement and accuracy
  of the mouse functions

*)

procedure TestMouse(MouseStyle : integer);
var
  points : Array[0..19] of TPoint;
  i : integer;

begin

  points := GetMousePoints();

  for i := 0 to 19 do
  begin
    // writeln('Testing point: ' + toStr(points[i].X) + ', ' + toStr(points[i].Y));

    case MouseStyle of
      1:
        begin
          writeln('SimpleMouse()...');
          SimpleMouse(points[i].X, points[i].Y, 5, 5);
        end;

      2:
        begin
          writeln('EquationMouse()...');
          EquationMouse(points[i].X, points[i].Y, 10);
        end;

      3:
        begin
          writeln('RelativeMouse()...');
          RelativeMouse(points[i].X, points[i].Y, 10);
        end;
      4:
        begin
          writeln('RandomSpeedMouse()...');
          RandomSpeedMouse(points[i].X, points[i].Y, 7);
        end;

    end; // end case MouseStyle

  end; // end for

end; // end Testmouse()


// Mouse style Constants
const
SIMPLE = 1;
EQUATION = 2;
RELATIVE = 3;
RANDOM_SPEED = 4;

var
loops : integer;

begin
  ClearDebug();
  ActivateClient();

  for loops := 0 to 20 do
  begin
    TestMouse(4);
  end;


end.
